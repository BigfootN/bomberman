#ifndef CHOICE_IP_LOOP_H
#define CHOICE_IP_LOOP_H

#include "types.h"

int choice_menu_choice_ip(control_t*, SDL_Event);
void select_data(control_t* control);
void events_loop_ip_choice(control_t*, int*);

#endif
