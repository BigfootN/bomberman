#ifndef GAME_TIME_H
#define GAME_TIME_H

#include "types.h"
#include "define.h"

int convert(int time, int number, int flag);
void display_time(control_t *control);
void display_counter(control_t *, int, int, int, int);

#endif
