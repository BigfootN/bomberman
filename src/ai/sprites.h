#ifndef SPRITES_H
#define SPRITES_H

#include "types.h"

int central_init_sprites(control_t*);
int init_sprites(control_t*);
int init_menu(control_t*);
int init_banner(control_t*);
int init_intro(control_t*);
int init_font(control_t*);
int init_wait(control_t*);
int init_result_panel(control_t*);

#endif
