/*#include <stdio.h>
#include <stdlib.h>

int foo(int x) {
    if (x < 10) {
        return 100;
    }
    return 1000;
}

int main_old() {
    char *name = (char *) malloc(sizeof(char) * 10);
    name[9] = 'x';

    int var;
    var = 0;
    var = foo(var);

    free(name);

    return 0;
}*/